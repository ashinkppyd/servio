apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: django-backend
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: backend
  namespaceSelector:
    matchNames:
      - servio
  endpoints:
    - port: http
      path: /metrics
      interval: 15s